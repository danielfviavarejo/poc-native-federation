export interface OpcoesCamera {
    pathSuccess: string,
    origin?: string,
    callBackUrl: string,
    screenTitle?: string,
    description?: string,
    showSecondaryButton: boolean,
    secondaryButtonTitle?: string,
    delayMsg?: string,
    delayTime: number
}